# AGENTS.md

## 目的
このリポジトリでは、まず現行ファイル構成とユーザー要求に整合する変更を優先してください。明示的な依頼がない限り、不要な実装追加は避けてください。

## リポジトリ構成
- `README.md`: プロジェクト概要。
- `docs/scenario/`: シナリオ文書。
- `docs/usecase/`: ユースケース文書。

## 整合性ルール（矛盾回避）
1. 記述と実ファイル構成が矛盾する場合は、実際のディレクトリ構成を優先して判断してください。
2. `README.md` に将来想定の構成（例: `src/`, `infra/`）が書かれていても、実在しない場合は存在前提で作業しないでください。
3. 不整合を見つけたら、ユーザー要求の範囲内でドキュメント（`README.md` または `AGENTS.md`）を最小修正してください。

## エージェント作業ルール
1. 変更は最小限にし、必ずユーザー要求の範囲に限定してください。
2. `docs/scenario/` と `docs/usecase/` の Markdown 文書の追加・更新を優先してください。
3. リポジトリ内で定義されていない build/test/runtime コマンドを作らないでください。
4. 実装ファイルの新規作成が必要な場合は、作成理由と最小構成を先に明記してください。

## ドキュメントスタイル
- 見出しは簡潔にし、セクションは短く保ってください。
- scenario 文書と usecase 文書で用語を統一してください。
- 大きな本文を重複させず、関連文書への相互リンクを優先してください。

## 参照
- プロジェクトルート: [README.md](README.md)
- Scenario 文書フォルダ: [docs/scenario](docs/scenario)
- Use case 文書フォルダ: [docs/usecase](docs/usecase)
